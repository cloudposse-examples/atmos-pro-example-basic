# yaml-language-server: $schema=https://atmos.tools/schemas/atmos/atmos-manifest/1.0/atmos-manifest.json

components:
  terraform:
    # This is an example of a Terraform component with some common values.
    # Here we can define default values that we can inherit in stacks and override as necessary.
    mycomponent:
      metadata:
        component: mock
      vars:
        foo: "foo catalog default"
        bar: "bar catalog default"
        baz: "baz catalog default"
